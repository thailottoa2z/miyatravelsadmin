## Packages
recharts | For financial charts and dashboards (if needed for the cash tile)
framer-motion | For smooth transitions and animations (modals, page loads)
date-fns | For date formatting in tables and inputs
lucide-react | For icons (already in base stack, but confirming usage)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
